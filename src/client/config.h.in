#ifndef CONFIG_H
#define CONFIG_H

// Konfiguracni soubor klienta, ktery je jeste preparsovan pomoci CMake

// Verze se ziska z CMake
#define CRYO_CLIENT_VERSION_MAJOR @cryo_client_VERSION_MAJOR@
#define CRYO_CLIENT_VERSION_MINOR @cryo_client_VERSION_MINOR@
#define CRYO_CLIENT_VERSION_REVISION @cryo_client_VERSION_REVISION@
#define CRYO_CLIENT_VERSION @cryo_client_VERSION@

// Jmeno aplikace
const char *APP_NAME = "Cryo";

// Defaultni rozliseni aplikace
const int APP_WIDTH = 640;
const int APP_HEIGHT = 480;

const int FRAMERATE_LIMIT = 30;

// Je defaultne zapnuty fullscreen?
const bool APP_FULLSCREEN = false;

#endif
